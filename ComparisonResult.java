import java.util.*;
import java.io.*;

public class ComparisonResult{
    
    private ArrayList<String> list;
    
    public ComparisonResult(ArrayList<String> list) {
        this.list = list;
    }

    public sortElements() {

        System.out.println("sort the the strings in the list so that each word/character seperated by a space is individualized");
        
    }
    
    public compare(ComparisonResult list, ArrayList<String> ignores) {

        System.out.println("Compare the two elemenet lists (call the first method on the parameter list object), as well, don't add to sum if characters are in ignore list");
    
    }

}
